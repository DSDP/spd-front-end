import {
  sparlLinkify
} from 'sparl-core/helpers/sparl-linkify';

module('SparlLinkifyHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = sparlLinkify(42);
  ok(result);
});
